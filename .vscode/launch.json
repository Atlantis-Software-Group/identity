{
    "configurations": [
        {
            "name": "Identity",
            "type": "docker",
            "request": "attach",
            "platform": "netCore",
            "processName": "asg.identity",
            "sourceFileMap": {
                "/app/src": "${workspaceFolder}/src/asg.identity"
            }
        }
    ]
}